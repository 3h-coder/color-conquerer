"""
Handlers are objects used to orchestrate the client-server communication lifecycle.

For example, the RoomHandler is responsible for managing the rooms where matches are played,
while the MatchHandler is responsible for managing the matches themselves.
"""
