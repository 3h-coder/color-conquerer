export enum Events {
    QUEUE_REGISTER = "queue-register",
    QUEUE_REGISTERED = "queue-registered",
    QUEUE_WITHDRAWAL = "queue-withdrawal",
    QUEUE_OPPONENT_FOUND = "queue-opponentFound",
    MATCH_OPPONENT_LEFT = "match-opponentLeft",
    MATCH_READY = "match-ready"
}
